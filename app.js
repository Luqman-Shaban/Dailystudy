function Monday() {
    lessons = ['DOM','Objects','Loops','functions']
    computer = lessons[Math.floor(Math.random() * lessons.length)]
    console.log(computer);
}

function Tuesday() {
    lessons = ['DOM','Objects','Loops','functions']
    computer = lessons[Math.floor(Math.random() * lessons.length)]
    console.log(computer);
}

function Wednseday() {
    lessons = ['DOM','Objects','Loops','functions']
    computer = lessons[Math.floor(Math.random() * lessons.length)]
    console.log(computer);
}

function Thursday() {
    lessons = ['DOM','Objects','Loops','functions']
    computer = lessons[Math.floor(Math.random() * lessons.length)]
    console.log(computer);
}

function Friday() {
    lessons = ['DOM','Objects','Loops','functions']
    computer = lessons[Math.floor(Math.random() * lessons.length)]
    console.log(computer);
}

function Saturday() {
    lessons = ['DOM','Objects','Loops','functions']
    computer = lessons[Math.floor(Math.random() * lessons.length)]
    console.log(computer);
}


function Sunday() {
    lessons = ['DOM','Objects','Loops','functions']
    computer = lessons[Math.floor(Math.random() * lessons.length)]
    console.log(computer);
}

//The code works by invoking a function using the day of the week's name


